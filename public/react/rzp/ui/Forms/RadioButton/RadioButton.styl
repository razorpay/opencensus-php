@import '../../../../styles/rzp/vars.styl'

.RadioButton{
  margin: 16px 0;

  label {
    padding-left: 0;
    font-weight: normal;
    display: block;
  }

  input {
    display: none;

    &:checked + div {
      .RadioButton__button {
        &:before {
          opacity: 1;
          transform: rotate(42deg) translate(8px, -3px) scale(1);
        }
      }
    }
  }

  &__button {
    width: 25px;
    height: @width;
    background: #f5f5f5;
    border: 1px solid #E0E0E0;
    border-radius: 50%;
    float: left;
    cursor: pointer;

    &:before {
      content: '';
      position: absolute;
      width: 11px;
      height: 11px;
      margin-left: -2px;
      margin-top: 3px;
      border-radius: 50%;
      background-color: $primary-color;
      opacity: 0;
      transition: 0.2s;
      transform: rotate(42deg) translate(8px, -3px) scale(0.4);
    }
  }

  &__label {
    padding-left: 32px;
    line-height: 24px;
  }
}
