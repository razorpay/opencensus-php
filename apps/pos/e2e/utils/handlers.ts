import { landingPageMocks } from '../suites/landing-sales-agent/mocks/handlers';

/**
 * Global app level handlers needed for config
 */
const handlers = [...landingPageMocks];

export default handlers;
