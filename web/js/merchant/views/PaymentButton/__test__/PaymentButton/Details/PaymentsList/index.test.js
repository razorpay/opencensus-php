import { App } from 'merchant/views/PaymentButton/__test__/mocks/fixtures/PaymentList';

describe('Payment Button PaymentList Index - UT', () => {
  test('should have payment button payment list app component defined', () => {
    expect(App).toBeDefined();
  });
});
