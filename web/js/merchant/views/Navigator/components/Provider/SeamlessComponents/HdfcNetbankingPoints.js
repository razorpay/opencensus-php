import React from 'react';
import { List, ListItem, ListItemText } from '@razorpay/blade/components';

export const HdfcNetbankingPoints = () => {
  return (
    <List size="small" variant="unordered">
      <ListItem>
        <ListItemText color="surface.text.gray.subtle">
          Reach out to your relationship or account manager to get your integration kit with
          credentials for a direct settlement with refunds type terminal
        </ListItemText>
      </ListItem>
    </List>
  );
};
