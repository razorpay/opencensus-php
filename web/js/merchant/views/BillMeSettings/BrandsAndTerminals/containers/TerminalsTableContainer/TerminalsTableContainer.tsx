import React from 'react';
import { Box } from '@razorpay/blade/components';

const TerminalsTableContainer = (): React.ReactElement => (
  <Box padding="spacing.7">Terminals List</Box>
);

export default TerminalsTableContainer;
