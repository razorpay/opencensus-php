import React from 'react';
import BankDetails from './index';

export default {
  title: 'Onboarding/BankDetails',
  component: BankDetails,
};

const Template = () => <BankDetails />;

export const Default = Template.bind({});
