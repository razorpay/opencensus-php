import React from 'react';
import BusinessOverview from './index';

export default {
  title: 'Onboarding/BusinessOverview',
  component: BusinessOverview,
};

const Template = () => <BusinessOverview />;

export const Default = Template.bind({});
