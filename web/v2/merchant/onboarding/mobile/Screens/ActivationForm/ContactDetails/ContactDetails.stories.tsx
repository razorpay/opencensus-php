import React from 'react';
import ContactDetails from './index';

export default {
  title: 'Onboarding/ContactDetails',
  component: ContactDetails,
};

const Template = () => <ContactDetails />;

export const Default = Template.bind({});
