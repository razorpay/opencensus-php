import React from 'react';
import BusinessDetails from './index';

export default {
  title: 'Onboarding/BusinessDetails',
  component: BusinessDetails,
};

const Template = () => <BusinessDetails />;

export const Default = Template.bind({});
