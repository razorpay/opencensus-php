<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="google" value="notranslate" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Razorpay Merchant Dashboard">
    <meta name="author" content="Razorpay">
    <link rel="shortcut icon" href="{{asset('img/favicon.png')}}">
    <title>Razorpay - Dashboard</title>
    @section('css')
        <!-- Custom styles for this template -->
    <link href="{{asset('css/lib/bootstrap.reset.css')}}" rel="stylesheet">
    <link href="{{asset('css/lib/bootstrap.min.css')}}" rel="stylesheet">
    

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
    <link href="{{asset('css/<!-- @echo DEST -->/style.css')}}" type="text/css" rel="stylesheet" />
    @show
    <script type="text/javascript" src="{{asset('js/<!-- @echo DEST -->/pre.js')}}"></script>
</head>
<body>
    <header>
        <div id="nav" class="container grid grid-pad">
            <div id="logo" class="col-1-3">
                <a href="/"><img src="{{asset('img/logo.png')}}" alt="Razorpay"></a>
            </div>
            <ul id="centernav" class="col-1-3">
                <li></li>
                <li></li>
                <li></li>
            </ul>
            <ul id="rightnav" class="col-1-3">
                @if (\Auth::admin()->check() === true)
                    <li class="button"><a href="/admin/logout">Sign Out</a></li>
                @else
                    <li class="button"><a href="/admin/login">Sign In</a></li>
                @endif
            </ul>
        </div>
    </header>
    <div id="main">
        @if (\Auth::admin()->check() !== true)
            @yield('content')
        @else
            <div class="grid grid-pad">
            <div class="col-1-4 spaced-right-box">
                @include('admin.sidebar')
            </div>
            <div class="col-9-12 spaced-right-box">
                <div class="content boxed" id="dashboard-wrapper">
                    <div id="dashboard" class="panel">
                        @yield('panelcontent')
                    </div>
                </div>
            </div>
            </div>
        @endif
    </div>
    @section('js')
    <script type="text/javascript" src="{{asset('js/lib/bootstrap.min.js')}}"></script>
    @show
</body>
</html>