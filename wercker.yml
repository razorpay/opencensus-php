box:
  id: razorpay/onggi:php-7.2
  username: $DOCKERHUB_USER
  password: $DOCKERHUB_PASSWORD
command-timeout: 15
dev:
  steps:
    - internal/shell:
      code: echo "Welcome to Razorpay\\Api"
build:
    services:
      - id: mysql
        tag: 5.7
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: root
      - redis
      - id: elasticsearch
        tag: 5.2-alpine
    # The steps that will be executed on build
    steps:
      - script:
          name: copy env file
          code: cp ./environment/.env.wercker ./environment/.env.testing
      - add-ssh-key:
          keyname: RZP_API
      - script:
          name: Export ES env var to API environment variables
          code: >
              export ES_HOST="$ELASTICSEARCH_PORT_9200_TCP_ADDR"
              export ES_AUDIT_HOST="$ELASTICSEARCH_PORT_9200_TCP_ADDR"
      - script:
          name: Export wercker DB env var to API environment variables
          code: >
              export DB_LIVE_HOST="$MYSQL_PORT_3306_TCP_ADDR"
              export DB_LIVE_PORT="$MYSQL_PORT_3306_TCP_PORT"
              export DB_TEST_HOST="$MYSQL_PORT_3306_TCP_ADDR"
              export DB_TEST_PORT="$MYSQL_PORT_3306_TCP_PORT"
              export DB_AUTH_HOST="$MYSQL_PORT_3306_TCP_ADDR"
              export DB_AUTH_PORT="$MYSQL_PORT_3306_TCP_PORT"
              export SLAVE_DB_LIVE_HOST="$MYSQL_PORT_3306_TCP_ADDR"
              export SLAVE_DB_LIVE_PORT="$MYSQL_PORT_3306_TCP_PORT"
              export SLAVE_DB_TEST_HOST="$MYSQL_PORT_3306_TCP_ADDR"
              export SLAVE_DB_TEST_PORT="$MYSQL_PORT_3306_TCP_PORT"
      - script:
          name: Export wercker Redis env var to API environment variables
          code: >
              export REDIS_HOST="$REDIS_PORT_6379_TCP_ADDR"
              export REDIS_PORT="6379"
              export REDIS_LABS_HOST="$REDIS_PORT_6379_TCP_ADDR"
              export REDIS_LABS_PORT="6379"
      - create-file:
          name: Create assertion.ini
          filename: /etc/php7/conf.d/assertion.ini
          overwrite: true
          content: |
              zend.assertions=1
              assert.exception=1
      - script:
          name: Composer set-up
          code: |
              composer config -g github-oauth.github.com $github_token
              export COMPOSER_CACHE_DIR="$WERCKER_CACHE_DIR/composer"
              mkdir -p $COMPOSER_CACHE_DIR
              composer install --no-interaction --optimize-autoloader
      - script:
          name: Create certificate directories
          code: |
              mkdir -p /opt/razorpay/certs/first_data
      - script:
          name: Run unit tests
          code: php -d memory_limit=512M vendor/phpunit/phpunit/phpunit tests/Functional/Batch/. --printer=Codedungeon\\PHPUnitPrettyResultPrinter\\Printer
      - script:
          name: create commit.txt
          code: git rev-parse HEAD > public/commit.txt
    after-steps:
      - slack-notifier:
          url: $SLACK_URL
          channel:  tech_bots
          username: wercker
          icon_url: https://cdn.rawgit.com/wantedly/step-pretty-slack-notify/master/icons/$WERCKER_RESULT.jpg
      - script:
          name: Save trace
          code: cp storage/logs/* $WERCKER_REPORT_ARTIFACTS_DIR/
deploy:
    box:
      id: razorpay/containers:php7
      username: $DOCKERHUB_USER
      password: $DOCKERHUB_PASSWORD
    steps:
      - script:
          name: Check if branch is master on prod-api
          code: |
            if [ "$APP_ENV" == "prod" ]; then
              if [ "$WERCKER_GIT_BRANCH" != "master" ]; then
                echo "[ERROR] Cannot deploy a non-master branch to prod"
                exit 1
              fi
            fi
      - script:
          name: check vendor dir exists
          code: if [ ! -d vendor ] ; then exit 1 ; fi;
      - script:
          name: check if dotfile rsyncignore exists
          code: if [ ! -f .rsyncignore ] ; then exit 1 ; fi;
      - script:
          name: Conditionally verify if commit matches between K8s and Asg.
          code: |
            if [ "$APP_ENV" == "prod" ] && [ "$CHECK_K8S_COMMIT" == "true" ]; then
              K8S_COMMIT=`curl https://k8s-prod-api.razorpay.com/commit.txt`
              if [ "$K8S_COMMIT" != "$WERCKER_GIT_COMMIT" ]; then
                echo "[ERROR] Kubernetes deployments enabled. Please deploy to K8s via falcon before deploying here."
                exit 1
              fi
            fi
      - razorpay/aws-code-deploy@0.0.32:
          key: $AWS_KEY
          secret: $AWS_SECRET
          region: $AWS_REGION
          application-name: api
          deployment-group-name: $WERCKER_DEPLOYTARGET_NAME
          s3-bucket: rzp-code-deploy
      - script:
          name: Conditionally skip newrelic deployment marker
          code: |
            if [ "$APP_ENV" == "prod" ]; then
              SKIP_NR=false
            fi
      - hashfyre/newrelic-deployment:
          api_key: $NEWRELIC_API_KEY
          app_name: "$APP_ENV-$APP_NAME"
          skip: $SKIP_NR
    after-steps:
      - slack-notifier:
          url: $SLACK_URL
          channel: tech_bots
          username: wercker
          icon_url: https://cdn.rawgit.com/wantedly/step-pretty-slack-notify/master/icons/$WERCKER_RESULT.jpg
      - script:
         name: post_deploy_notifier
         code: |
            python scripts/post_deploy_notifier.py
