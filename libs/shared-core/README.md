# shared-core

This library was generated with [Nx](https://nx.dev).

No imports from other part of the app should be made inside it. It should be always decoupled, and should work on its own.

## Building

Run `nx build shared-core` to build the library.
