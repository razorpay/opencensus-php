import React from 'react';
import { APP_CONSTANTS } from '@apps/{{ appName }}/src/constants';
import { AppContainer } from '@apps/{{ appName }}/src/container';
import { Heading, Text } from '@razorpay/blade/components';
import { toTitleCase } from "@libs/shared-utils";

export const App = () => {
  return (
    <AppContainer>
      <Heading>Welcome {toTitleCase(APP_CONSTANTS.APP_NAME.split("-").join(" "))}!</Heading>
      <Text variant="caption">This app is generated by dashboard-cli. Happy coding!</Text>
    </AppContainer>
  );
};

