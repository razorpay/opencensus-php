apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  generation: 1
  name: func
  namespace: func
spec:
  rules:
  - host: func-dashboard.razorpay.in
    http:
      paths:
      - backend:
          serviceName: func
          servicePort: 80
      path: /
